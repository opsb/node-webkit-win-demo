# node-webkit windows demo

# Setup

    $ npm install
    $ npm install -g flatten-packages

# Running

    # npm start

# Building for OSX/windows

    $ flatten-packages
    $ npm run-script build

Builds are generated and stored in ./builds